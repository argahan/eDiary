@model IEnumerable<E_GUNLUK.Models.Note>

@{ 
    Layout = null;
}
@if (Model.Count() == 0)
{
    <h3>No Posts</h3>
}
else
{
    <h3>@Model.Count() Posts</h3>
<ul>
    @foreach (var item in Model)
    {
        <li><a href="/Notes/Details/@item.NoteId">@item.Title</a></li>
    }
</ul>
}