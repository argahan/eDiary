@model IEnumerable<E_GUNLUK.Models.Comments>

@{ 
    Layout = null;
}

@{
    if (Model.Count() > 0)
    {
        <span>@Model.Count() Comments</span>
    }
    else
    {
        <span>0 Comments</span>
    }
}